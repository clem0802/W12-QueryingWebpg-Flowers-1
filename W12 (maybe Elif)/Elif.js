// Elif 2022.02.03 (W12) EXO

for (var i = 0; i < allButtons.length; i++) {
    allButtons[i].classList.toggle('all-buttons');
}

// grab it from the DOM and toggle it
.style-btn

let image = document.querySelector("img");
image.classList.toggle("img-improved");

let buttonTexte = document.querySelector(".style-btn");
if(buttonTexte.innerHTML === "Change to Improved Style"){
    buttonTexte.innerHTML = "Normal design";
} else{
    buttonTexte.innerHTML = "RChange to Improved Style";
}